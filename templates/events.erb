<%= "*#{date.strftime('%m').gsub(/\A0/, '')}月#{date.strftime('%d').gsub(/\A0/, '')}日(#{date.wday_name})*" %>

<% if events.count > 0 %>
<% events.each do |event| %>
<%= event[:emoticon] %> <%= event[:period] %> <%= event[:plan] %> <%= event[:private] ? ':lock: ' : '' %><%= event[:title] %> <%= event[:facilities] %> <%= event[:url] %> 
<% end %>
<% else %>
予定はありません。
<% end %>
